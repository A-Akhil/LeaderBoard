# Frontend Environment Variables
VITE_BASE_URL=http://localhost:4000/api

# Note: Only VITE_ prefixed variables are exposed to the client
# Do not include sensitive information in frontend environment variables